
#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    vector<int>v;
int n;cin>>n;
    for(int i=0;i<n;i++){
        int tt,x;cin>>tt;
        if(tt==1){
            cin>>x;
            v.push_back(x);
        }
        else{
            if(!v.empty()){
           v.pop_back(); 
        }
    }
    }
    if(v.empty()){
        cout<<"EMPTY";
    }else 
       for(int x:v)cout<<x<<' ';
    
    return 0;
}
